ToDo for XCat 0.5:
   - (re)Implement the doc() function
   - Rate limiting
     * max_concurrent_connections flag
     * global speed limit?
   - File shell
   - CWD
   - HTTP Status code handling for true/false/error conditions
